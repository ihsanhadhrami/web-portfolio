# Public site metadata (safe to expose to the client)
VITE_SITE_URL="https://ihsanhadhrami.com"

# The contact form submits to Netlify Forms. No configuration needed here —
# vite.config.ts detects Netlify's own build environment automatically.
